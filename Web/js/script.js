/**
 * script.js — versão antiga usada só em protótipos com submit HTML puro.
 *
 * O fluxo atual do projeto usa `web-app.js` + API Flask (`/api/...`).
 * Os formulários em login/cadastro agora são enviados via fetch com sessão.
 *
 * Mantido no repositório para histórico; as páginas linkam `/js/web-app.js`.
 */

/* eslint-disable no-unused-vars */
document.addEventListener("DOMContentLoaded", () => {
  console.info("COE: use web-app.js nas páginas atuais.");
});
